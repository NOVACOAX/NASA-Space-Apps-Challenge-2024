// This module contains all the custom configs for extending Tailwind.

/**
 * @description - Contains all custom properties for extending Tailwind's theme.
 */
const customThemeConfig = {
  colors: require('./colors'),
}

module.exports = customThemeConfig
